Um ein XML-Dokument gegen das TSD-Schema zu validieren, kann folgender
Befehl genutzt werden:
 xmllint example_product.xml --schema doc/TSD_1.1_Schemas_HSRM/tsd/ProductData.xsd

* Test
* Beim Deploy (derzeit alles im "Production-Environment"), müssen die Assets
  vorkompiliert werden:
   rake assets:precompile
